{
  "name": "alser-landing",
  "version": "1.0.0",
  "description": "Requires [Node.js](https://nodejs.org/) to run.",
  "main": "index.js",
  "scripts": {
    "watch:sass": "node-sass src/scss/style.scss src/css/style.css -w",
    "devserver": "live-server --open=src/",
    "start": "npm-run-all --parallel devserver watch:sass"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "node-sass": "^5.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
